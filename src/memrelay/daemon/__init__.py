"""Background daemon: owns Graphiti + Kuzu, tails sessions, serves the query API.

Skeleton package for the SPEC §8 layout. Implemented in the daemon/observation
epic; E0 only proves the traceforge ingestion path (see ``memrelay.ingest``).
"""
